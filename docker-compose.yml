version: '3'

services:
    web:
        image: nginx:mainline
        ports:
            - "80:80"
        volumes:
            - ./default.conf:/etc/nginx/conf.d/default.conf
            - .:/var/www/html
        links:
            - php
    php:
        build: .
